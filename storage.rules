rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Invoice PDFs: owner-only read/write
    match /invoices/{ownerId}/{allPaths=**} {
      allow read: if request.auth != null && request.auth.uid == ownerId;
      allow write: if request.auth != null && request.auth.uid == ownerId;
    }

    // Signatures: public read (shown on shared invoice pages), owner-only write
    match /signatures/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    // User assets (logo, UPI QR): public read (shown on invoices/PDFs), owner-only write
    match /users/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    // Default: deny all
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
